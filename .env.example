# URL del proxy serverless (Cloudflare Worker) per l'assistente AI.
# In sviluppo locale (wrangler dev) tipicamente:
#   VITE_PROXY_URL=http://localhost:8787
# In produzione: l'URL del Worker deployato.
VITE_PROXY_URL=
